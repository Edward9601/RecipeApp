export enum RecipeType {
    RECIPE = 'recipe',
    SUB_RECIPE = 'sub_recipe'
}

export enum RecipeObjectType{
    INGREDIENT = 'ingredient',
    STEP = 'step',
    CATEGORY = 'category',
    TAG = 'tag',
    IMAGE = 'image'
}